Convert the user's vehicle-safety question into a JSON query for the NHTSA API.

Output only a JSON object, nothing else.

Schema:
{"endpoint": "recalls|complaints|safetyRatings", "make": "...", "model": "...", "year": "..."}

If the question cannot be turned into a query, output {"error": "..."}.
